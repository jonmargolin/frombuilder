export const variableOptions = ['Country', 'City', 'Devices', 'Session Count'];
export const operatorOptions = ['Equals', 'Does Not Equal', 'At least', 'Lower than', 'Is', "Isn't", 'Contain', 'Does Not Contain'];
export enum FieldName {
    country = 'Country',
    city = 'City',
    devices = 'Devices',
    sessionCount = 'Session Count',
}
export enum OperatorOptions {
    equals = 'Equals',
    doesNotEqual = 'Does Not Equal',
    atLeast = 'At least',
    lowerThan = 'Lower than',
    is = 'Is',
    IsNot = 'Is Not',
    contain = 'Contain',
    doesNotContain = 'Does Not Contain',
}
export const numbersOptions = ['Equals', 'DoesNotEqual', 'AtLeast', 'LowerThan'];
export const baseApi = 'https://countriesnow.space/api/v0.1/';
